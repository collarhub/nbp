<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace= "userMapper" >
    <select id ="selectUser" parameterType="java.util.HashMap" resultType= "java.util.HashMap">
        select *
        from user
        where user_phone = #{user_phone}
    </select>
    <insert id ="insertUser" parameterType="com.nbp.simsns.vo.UserVO" >
        insert into user
        (user_email, user_name, user_phone, user_address)
        values
        ( #{userEmail}, #{userName}, #{userPhone}, #{userAddress} )
    </insert>
</mapper>